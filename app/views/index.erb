<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title></title>
  </head>
  <body>
    <p>
      <% @posts.each do |post| %>
        <%= post.name %>
        <%= post.content %>
      <% end %>
    </p>

    <p>
      <%= "#{DELETED_POSTS.last} was deleted" %>
    </p>
  </body>
</html>
